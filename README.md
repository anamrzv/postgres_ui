# PostgreSQL UI

Современный веб-интерфейс для управления PostgreSQL базой данных с удобным редактированием таблиц и аналитикой.

## 🚀 Возможности

- **Управление таблицами**: Просмотр, добавление, редактирование и удаление записей
- **User-friendly интерфейс**: Работа с данными через удобные формы, а не прямое редактирование таблиц
- **Аналитика и отчеты**: Визуализация данных через диаграммы и графики
- **Поиск и фильтрация**: Быстрый поиск по данным с фильтрами
- **Пагинация**: Эффективная работа с большими объемами данных
- **Масштабируемость**: Архитектура позволяет легко добавлять новые функции

## 📁 Структура проекта

```
postgres_ui/
├── backend/                 # Node.js Backend API
│   ├── src/
│   │   ├── config/         # Конфигурация (БД и т.д.)
│   │   ├── controllers/    # Контроллеры (бизнес-логика)
│   │   ├── routes/         # API маршруты
│   │   ├── middleware/     # Middleware (валидация, обработка ошибок)
│   │   └── server.js       # Точка входа
│   ├── .env.example
│   └── package.json
│
├── frontend/                # Vue.js Frontend
│   ├── src/
│   │   ├── api/            # API клиенты
│   │   ├── components/     # Переиспользуемые компоненты
│   │   ├── layouts/        # Layouts (sidebar, header)
│   │   ├── router/         # Vue Router
│   │   ├── stores/         # Pinia stores
│   │   ├── views/          # Страницы
│   │   └── main.js
│   ├── vite.config.js
│   └── package.json
│
└── README.md
```

## 🛠 Технологический стек

### Backend
- **Node.js** + **Express** - REST API
- **PostgreSQL** (pg) - Драйвер для работы с БД
- **Express Validator** - Валидация входных данных
- **Helmet** - Безопасность
- **Morgan** - Логирование
- **CORS** - Cross-Origin Resource Sharing

### Frontend
- **Vue 3** - Progressive JavaScript Framework
- **Vite** - Быстрый dev-сервер и сборщик
- **Vue Router** - Маршрутизация
- **Pinia** - Управление состоянием
- **Element Plus** - UI библиотека компонентов
- **Axios** - HTTP клиент
- **Chart.js** + **vue-chartjs** - Визуализация данных

## 📦 Установка

### Требования
- Node.js 18+
- PostgreSQL 12+
- npm или yarn

### Backend

```bash
cd backend

# Установить зависимости
npm install

# Создать .env файл из примера
cp .env.example .env

# Настроить переменные окружения в .env
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=your_database
# DB_USER=your_user
# DB_PASSWORD=your_password
# PORT=3000
```

### Frontend

```bash
cd frontend

# Установить зависимости
npm install
```

## 🚀 Запуск

### Backend

```bash
cd backend

# Development режим с автоперезагрузкой
npm run dev

# Production режим
npm start
```

Backend будет доступен на `http://localhost:3000`

### Frontend

```bash
cd frontend

# Development режим
npm run dev

# Build для production
npm run build
```

Frontend будет доступен на `http://localhost:5173`

## 📚 API Endpoints

### Tables
- `GET /api/tables` - Получить список всех таблиц
- `GET /api/tables/:tableName/schema` - Получить схему таблицы
- `GET /api/tables/:tableName/metadata` - Получить метаданные

### Data (CRUD операции)
- `GET /api/data/:tableName` - Получить данные (с пагинацией, фильтрами)
- `GET /api/data/:tableName/:id` - Получить одну запись
- `POST /api/data/:tableName` - Создать запись
- `PUT /api/data/:tableName/:id` - Обновить запись
- `DELETE /api/data/:tableName/:id` - Удалить запись
- `POST /api/data/:tableName/bulk` - Массовое создание

### Analytics
- `POST /api/analytics/aggregate` - Агрегированные данные
- `POST /api/analytics/chart` - Данные для графиков
- `POST /api/analytics/query` - Выполнить custom SQL query
- `GET /api/analytics/:tableName/stats` - Статистика таблицы

## 🎨 Основные страницы

1. **Dashboard** - Общая статистика и быстрый доступ
2. **Список таблиц** - Просмотр всех таблиц в БД
3. **Редактор таблицы** - CRUD операции с записями
4. **Аналитика** - Графики и отчеты по данным

## 🔒 Безопасность

- Валидация всех входных данных
- Защита от SQL injection через параметризованные запросы
- Helmet для защиты HTTP заголовков
- CORS настройки
- Ограничение типов SQL запросов в analytics endpoints

## 🔧 Конфигурация

### Backend (.env)
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
```

### Frontend (vite.config.js)
```javascript
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:3000',
      changeOrigin: true
    }
  }
}
```

## 📝 Примеры использования

### Добавление новой записи
1. Откройте список таблиц
2. Выберите нужную таблицу
3. Нажмите "Добавить запись"
4. Заполните форму
5. Сохраните

### Просмотр аналитики
1. Перейдите в раздел "Аналитика"
2. Выберите таблицу
3. Настройте параметры визуализации
4. Просмотрите графики и статистику

## 🚧 Дальнейшее развитие

- [ ] Аутентификация и авторизация пользователей
- [ ] Расширенные возможности фильтрации
- [ ] Экспорт данных (CSV, Excel, JSON)
- [ ] Импорт данных из файлов
- [ ] Кастомные формы для разных таблиц
- [ ] История изменений (audit log)
- [ ] Более продвинутая аналитика
- [ ] Поддержка связей между таблицами (foreign keys)
- [ ] Темная тема интерфейса

## 📄 Лицензия

ISC

## 👥 Автор

Создано для удобного управления PostgreSQL базами данных.
